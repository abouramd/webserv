#include "responses.hpp"

std::string cgi(Client &client)
{
    // srand((unsigned) time(NULL));
    // std::string filename = static_cast<std::string>(srand((unsigned) time(NULL)));
    // std::cout << filename << std::endl;
    // return filename;
}